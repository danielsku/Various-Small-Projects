**FIFO_in_C** -> An automated game where players take turn rolling a dice. The turn changing mechanism is controlled by the FIFO


**TCP_IP_tests** -> A local command-line chat application where clients can connect and chat via terminal with the server being responsible to broad cast all messages


**spring-boot-hibernate-jpa-crud** -> Practice using Hibernate with entityManager and JPA to implement CRUD operations on a MySQL database


**spring-boot-rest-crud** -> Creating REST APIs endpoints that run CRUD operations on a MySQL database


**spring-boot-rest-security** -> Limiting REST endpoints to specific users based on their roles
